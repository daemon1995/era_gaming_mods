ZVSE2


; 4-6  harpies -> 6-8 harpies


!?FU(OnAfterErmInstructions);
;!UN:P()/?(wogOpt:y); Под будущее новое меню вог-настроек
!!VR(wogOpt:y):S(TRUE);

!!FU&(wogOpt)=0:E;

; Change starting army
!!re i/(HERO_LORELEI)/(HERO_DARKSTORN);
  !!re j/0/2; max 3 stacks
    !!HEi:C0/j/?(monType:y)/d;
    ;!IF:M^monType= %(monType)^;
    !!if&(monType)=(MON_HARPY);
      !!VR(monQty:y):S6 R2;
      !!HEi:C0/j/(MON_HARPY)/(monQty);
    !!en;
  !!en;
!!en;
